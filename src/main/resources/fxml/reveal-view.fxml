<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<VBox xmlns:fx="http://javafx.com/fxml/1" xmlns="http://javafx.com/javafx/21"
      fx:controller="app.wishlist.controller.RevealController"
      styleClass="container-white" spacing="20" alignment="TOP_CENTER">

    <HBox alignment="CENTER_LEFT">
        <Button text="← Back" onAction="#navigateBack" styleClass="button-back"/>
    </HBox>

    <VBox fx:id="waitingBox" alignment="CENTER" spacing="20">
        <Label text="🎅 Secret Santa Event" styleClass="reveal-header"/>
        <Label text="The draw has not happened yet." styleClass="reveal-waiting-text"/>
        <Label text="Check back later!" styleClass="reveal-waiting-subtext"/>
    </VBox>

    <VBox fx:id="resultBox" alignment="CENTER" spacing="30" visible="false">
        <Label text="You are the Secret Santa for..." styleClass="reveal-instruction"/>

        <Label fx:id="targetNameLabel" text="[NAME]"
               styleClass="reveal-target-name"/>

        <Button text="View Their Wishlist" onAction="#handleGoToWishlist"
                styleClass="button-success button-success-large"/>
    </VBox>

</VBox>
